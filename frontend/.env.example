# Frontend env — copy to frontend/.env.local for local dev.
# Only NEXT_PUBLIC_* vars are exposed to the browser.

NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
NEXT_PUBLIC_WS_URL=http://localhost:4000
